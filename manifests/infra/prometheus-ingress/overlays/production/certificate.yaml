apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus
spec:
  dnsNames:
  - prometheus.dev.cloudnativedays.jp
  secretName: cert-manager/wildcard-dev-cloudnativedays-jp
  issuerRef:
    name: prometheus-selfsigned
    kind: ClusterIssuer
